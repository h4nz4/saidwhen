## 1. RECOMMENDATION

**Don't build it.** "Sign in with Google" was already evaluated and **rejected today** (2026-07-08) as part of the accepted magic-link auth decision. Nothing in the request signals the one condition that decision named for revisiting — a significant user segment demanding social login. The evidence is same-day fresh, so this isn't stale: respect it, don't re-litigate.

The rejection rationale still holds on its face:
- Users are external freelancers with **no corporate IdP** to SSO against — Google login buys them nothing over an emailed link.
- OAuth adds console setup, credential rotation, and consent-screen/breakage surface that a **solo, few-hours-a-week maintainer** explicitly said is out ("anything that can break at 3am is out").
- Magic links already satisfy the only hard constraint from the owner: no stored passwords, no breach liability.

If the PM wants to proceed anyway, that's an owner-level reversal of an accepted decision, not an implementation ticket — it needs the delta answer below first.

## 2. PRIOR CONTEXT

All from the `knowledge/` OKF bundle:
- `knowledge/decisions/magic-link-auth.md` — **accepted** decision. Explicitly lists *"OAuth / 'Sign in with Google' SSO"* under **Rejected**, with the revisit trigger: *"if a significant user segment demands social login."*
- `knowledge/constraints/external-users.md` — users are individual freelancers, no Google Workspace/Entra/Okta to integrate.
- `knowledge/constraints/solo-maintainer.md` — one person, a few hours/week; minimize operational surface.
- `knowledge/interviews/2026-07-08-auth-scope.md` — owner (Ivan): no SSO need, ~zero auth maintenance, no passwords.

## 3. QUESTIONS

Since the wiki already answers "should we add OAuth" (no), I'd only ask the **delta / revisit-trigger** questions — the things that could actually overturn the decision:

1. What prompted this — is there a **concrete user segment now asking for Google login**, or is it speculative? (This is the exact revisit trigger the decision named.)
2. If real: how large, and are they asking for *convenience* or a hard *requirement*? Would that segment accept magic links instead?
3. Are you (owner) willing to take on the ongoing OAuth ops — Google Cloud console project, consent-screen verification, client-secret rotation — given the "nothing that breaks at 3am" bar you set today?
4. Has anything changed since this morning's scoping (compliance, a B2B/team-account direction) that invalidates the "individual freelancers, no corporate IdP" premise?

If the answers are "speculative / small / not willing / nothing changed," the recommendation stands and no code changes are warranted.

*(Did the evaluation; the wiki settles it. Want a full OAuth implementation plan regardless of the standing decision? Say so and I'll spec it.)*