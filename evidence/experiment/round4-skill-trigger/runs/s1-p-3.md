## 1. RECOMMENDATION

**Don't build it — this was already decided and rejected 5 hours ago, and the evidence is still fresh.**

TaskLite's authentication approach is a settled, `accepted` decision: single-use emailed magic links, and "Sign in with Google" OAuth SSO was *explicitly evaluated and rejected* in that same decision. The rejection rationale still holds verbatim:

- Users are external freelancers with **no corporate IdP** to integrate against — Google SSO buys them nothing over a magic link.
- Each OAuth provider adds console setup, credential rotation, and breakage surface a **solo maintainer** (a few hours/week, "anything that can break at 3am is out") can't absorb.

The decision was recorded `2026-07-08T10:00:00Z` — today. Nothing has changed circumstances, so per read-first this is a `status: accepted` decision with plausible evidence: respect it, don't re-litigate. The one documented condition for revisiting is *"a significant user segment demands social login"* — absent that signal, the answer is no.

## 2. PRIOR CONTEXT (what informed this)

- `knowledge/decisions/magic-link-auth.md` — accepted decision; magic-link auth chosen, **"OAuth / Sign in with Google SSO" listed under Rejected** with the exact reasons above and the revisit trigger.
- `knowledge/constraints/external-users.md` — users are individual freelancers, no Google Workspace/Entra/Okta to hook into.
- `knowledge/constraints/solo-maintainer.md` — one person, a few hours/week; minimize operational surface.
- `knowledge/interviews/2026-07-08-auth-scope.md` — owner (Ivan): "No [SSO]... no corporate identity provider," "Basically none" auth maintenance, "don't store passwords."

## 3. QUESTIONS (for the PM/owner)

I'm *not* asking the settled questions (who the users are, SSO need, maintenance budget) — the wiki answers those. Only the revisit-trigger question is open:

1. **Is there a concrete demand signal?** The decision says to revisit only if *"a significant user segment demands social login."* Do you have that — support requests, churn, a named cohort — or is this a speculative "let's add it"? (If speculative: YAGNI, close it.)
2. **What problem is SSO solving that magic links don't?** Magic links already give passwordless, no-storage login. If the pain is "second-device friction" or "link lands in spam," those are cheaper to fix than standing up an OAuth integration.
3. **Who owns the OAuth app long-term?** Google console project, consent-screen verification, client-secret rotation, and quota all land on the solo maintainer. Are you signing up for that recurring surface?

skipped: any design/estimate for the OAuth flow — add when a real demand signal clears question 1 and the decision is formally reopened.