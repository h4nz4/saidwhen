A new maintainer is joining PocketLedger, the project in this directory. Before touching code, they asked: "What are this project's binding constraints and why does each exist?" Answer from the project itself. Do not modify any files. Structure your answer with exactly these sections: 1. CONSTRAINTS AND WHY (each constraint with the reason it was chosen), 2. SOURCES (where each constraint is recorded, with paths), 3. OPEN QUESTIONS (what you could not determine from the project).
